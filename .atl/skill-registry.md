# Skill Registry — Calculadora

Auto-generated by `sdd-init` on 2026-05-01.

## User Skills (~/.claude/skills/)

| Skill | When to use |
|-------|-------------|
| branch-pr | PR creation workflow (only relevant if project becomes git-tracked) |
| go-testing | Go testing — NOT APPLICABLE to this vanilla JS project |
| issue-creation | GitHub issue creation (only if repo gets a remote) |
| judgment-day | Parallel adversarial review of changes |
| skill-creator | Creating new AI skills |

## Project Conventions

No project-level `CLAUDE.md`, `agents.md`, or `.cursorrules` detected at `C:\Personal\Calculadora`.
User-level `CLAUDE.md` applies globally: `C:\Users\Ideamos\.claude\CLAUDE.md`.

## Compact Rules (auto-resolved for sub-agent injection)

### Global (from user CLAUDE.md)
- Never add "Co-Authored-By" or AI attribution to commits. Conventional commits only.
- Never build after changes.
- Prefer `bat`/`rg`/`fd`/`sd`/`eza` over `cat`/`grep`/`find`/`sed`/`ls`.
- When asking a question, STOP and wait. Never assume answers.
- Verify technical claims before stating. Investigate when unsure.
- Always propose alternatives with tradeoffs when relevant.

### Project-specific (from README.md & docs/ARCHITECTURE.md)
- **Stack**: vanilla HTML/CSS/JS. NO build step, NO frameworks. Decision documented as DELIBERATE.
- **4-layer architecture** is non-negotiable:
  - `calculator.js` → pure math, no DOM, no Chart.js. IIFE with `window.Calculator`.
  - `ui.js` → DOM reads/writes, formatters (`fmtCOP`, `fmtCOPCuota`, `fmtPct`), `INPUT_CONFIG`.
  - `charts.js` → Chart.js wrappers, reads colors from CSS variables via `getComputedStyle`.
  - `app.js` → orchestrator. `recalcular()` is 5 lines. If it grows, refactor.
- **Result object** returned by `simulate()` is the contract — changing its shape requires updating `ui.js` and `charts.js`.
- **INPUT_CONFIG declarative pattern**: every input has `unit` and `format`. Add new inputs by extending this map plus `readInputs()`.
- **No tests**: validation is manual via known values in `docs/FORMULAS.md` §10 and visual browser check.
