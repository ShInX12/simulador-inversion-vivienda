# Dependencias (se regeneran con `npm install` — package-lock.json SÍ se versiona)
node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Cobertura de tests (vitest --coverage)
coverage/

# Variables de entorno (preventivo — no filtrar secretos)
.env
.env.local
.env.*.local

# Basura del sistema operativo
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini

# Configuración de editores (config personal, no del proyecto)
.vscode/
.idea/
*.swp
*.swo

# NOTA: openspec/ y .atl/ SÍ se versionan (decisión del equipo)
